# Flask no Azure App Service

Aplicação Flask simples publicada no Azure App Service com CI/CD via GitHub Actions.

## Rodar localmente
```bash
pip install -r requirements.txt
python app.py
```

Acesse em: http://localhost:8000
